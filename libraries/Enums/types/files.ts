export enum FileSortEnum {
  NAME = 'name',
  MODIFIED = 'modified',
  TYPE = 'type',
  SIZE = 'size',
}

export enum FileIconsEnum {
  CLOCK = 'clock',
  TRASH = 'trash',
  HEART = 'heart',
  FOLDER = 'folder',
  LINK = 'link',
}

export enum FileRouteEnum {
  DEFAULT = '',
  RECENT = 'recent',
  DELETED = 'deleted',
  FAVORITED = 'favorited',
  SHARED = 'shared',
  LINKS = 'links',
}
